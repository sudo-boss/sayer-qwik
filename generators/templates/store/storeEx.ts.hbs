import { {{constantCase name}} as {{camelCase name}} } from '../{{camelCase name}}'; // Importarlo en storeConfig.ts

export interface {{pascalCase name}} {
  someProp: string;
}

export const {{constantCase name}}: {{pascalCase name}} = {
  someProp: '',
};
