import { {{constantCase name}}_INIT as {{camelCase name}} } from '../{{camelCase name}}'; // Importarlo en storeConfig.ts

export interface {{pascalCase name}}State {
  someProp: string;
}

export const {{constantCase name}}_INIT: {{pascalCase name}} = {
  someProp: '',
};
