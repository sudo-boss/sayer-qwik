// ---Dependencies
import { component$ } from '@builder.io/qwik';
// import { useSelectorBuilder } from 'src/utils/hooks/styles/useSelectorBuilder';
// ---Styles
import style from './{{pascalCase name}}.module.scss';

interface Props {}

/**
 * {{pascalCase name}} Component:  Descripción del comportamiento...
 * @param {Props} props - Parámetros del componente como: ...
 */
export const {{pascalCase name}} = component$<Props>((props) =>  {
  // -----------------------CONSTS, HOOKS, STATES
  const { classNames } = useStylesBuilder(style.{{pascalCase name}});
  // -----------------------MAIN METHODS
  // -----------------------AUX METHODS
  // -----------------------RENDER
  return (
    <div className={classNames}>
      <p>{{pascalCase name}}</p>
    </div>
  );
})