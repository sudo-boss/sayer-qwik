// ---Dependencies
import { component$ } from '@builder.io/qwik';
import { useStyleBuilder } from 'src/utils/app/useStyleBuilder';
// ---Styles
import style from './{{pascalCase name}}.module.scss';

/**
 * {{pascalCase name}} Component:  Descripción del comportamiento...
 */
export const {{pascalCase name}} = component$((props) =>  {
  // -----------------------CONSTS, HOOKS, STATES
  const { classNames } = useStyleBuilder(style.{{pascalCase name}});
  // -----------------------MAIN METHODS
  // -----------------------AUX METHODS
  // -----------------------RENDER
  return (
    <div class={classNames.value}>
      <p>{{pascalCase name}}</p>
    </div>
  );
})